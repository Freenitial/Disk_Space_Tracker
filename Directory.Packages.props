<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup>
    <!-- Avalonia 12.1 nightly (dev line), from the nightly feed configured in NuGet.Config. -->
    <PackageVersion Include="Avalonia" Version="12.1.999-cibuild0067332-alpha" />
    <PackageVersion Include="Avalonia.Desktop" Version="12.1.999-cibuild0067332-alpha" />
    <PackageVersion Include="Avalonia.Themes.Fluent" Version="12.1.999-cibuild0067332-alpha" />
    <PackageVersion Include="Avalonia.Fonts.Inter" Version="12.1.999-cibuild0067332-alpha" />

    <!-- Static Skia + HarfBuzz (.lib) for NativeAOT single-file. This is the newest build
         published by 2ndLAB (frozen since 2025-11-04); it embeds SkiaSharp 3.119.x, which
         matches what Avalonia.Skia 12.x expects. -->
    <PackageVersion Include="2ndLAB.SkiaSharp.Static" Version="3.119.0-10.0.26100.0-1.1c7d845" />

    <PackageVersion Include="CommunityToolkit.Mvvm" Version="8.4.2" />

    <PackageVersion Include="Microsoft.Extensions.DependencyInjection" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Logging" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Console" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Debug" Version="10.0.7" />
  </ItemGroup>

  <!-- Test-only packages (consumed by Tests/DiskSpaceTracker.Tests.csproj). -->
  <ItemGroup>
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.5.1" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.0.1" />
    <PackageVersion Include="FluentAssertions" Version="6.12.2" />
  </ItemGroup>

</Project>
